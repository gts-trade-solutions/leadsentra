"use client";

// Stub kept so any remaining importers (e.g. legacy backup files) compile.
// The active multi-channel page is migrated to a placeholder; the real Facebook
// posting UI returns in Phase C of the Supabase -> MySQL migration.
export default function FacebookPanel(_props: any) {
  return null;
}
